---
phase: 02-react-app-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - index.html
  - src/App.jsx
  - src/hooks/useTheme.js
  - src/components/layout/Header.jsx
  - src/components/layout/Header.module.css
  - src/components/layout/TabBar.jsx
  - src/components/layout/TabBar.module.css
  - src/components/layout/ActionBar.jsx
  - src/components/layout/ActionBar.module.css
  - src/components/layout/SplitPane.jsx
  - src/components/layout/SplitPane.module.css
  - src/components/common/ProgressBar.jsx
  - src/components/common/ProgressBar.module.css
autonomous: true
requirements:
  - REACT-01
  - REACT-02
  - REACT-03
  - REACT-10
  - CSS-01
  - CSS-02
  - CSS-03

must_haves:
  truths:
    - "User sees a header with app title, theme toggle button, settings gear icon, debug toggle, and library toggle"
    - "User can switch between Create and Edit tabs and the left panel content changes"
    - "User can resize the split-pane and the ratio persists across page reloads"
    - "User can toggle dark/light theme and the choice persists across sessions"
    - "No theme flash on page load in dark mode"
    - "Action bar with disabled placeholder buttons and progress bar is always visible"
  artifacts:
    - path: "src/hooks/useTheme.js"
      provides: "Theme state management"
      exports: ["useTheme"]
    - path: "src/components/layout/Header.jsx"
      provides: "App header with controls"
      exports: ["default"]
    - path: "src/components/layout/TabBar.jsx"
      provides: "Tab navigation"
      exports: ["default"]
    - path: "src/components/layout/ActionBar.jsx"
      provides: "Action buttons and progress bar"
      exports: ["default"]
    - path: "src/components/layout/SplitPane.jsx"
      provides: "Resizable split-pane layout"
      exports: ["default"]
    - path: "src/components/common/ProgressBar.jsx"
      provides: "Reusable progress bar"
      exports: ["default"]
    - path: "src/App.jsx"
      provides: "Root layout wiring all components"
      exports: ["default"]
  key_links:
    - from: "src/App.jsx"
      to: "src/hooks/useTheme.js"
      via: "useTheme hook call"
      pattern: "useTheme"
    - from: "src/App.jsx"
      to: "src/components/layout/SplitPane.jsx"
      via: "SplitPane component with leftContent/rightContent props"
      pattern: "SplitPane"
    - from: "src/components/layout/SplitPane.jsx"
      to: "react-resizable-panels"
      via: "PanelGroup with autoSaveId for persistence"
      pattern: "autoSaveId"
---

<objective>
Build the React application shell layout: header with controls, tab bar, action bar, and resizable split-pane. Add theme switching with localStorage persistence and no flash on load.

Purpose: This is the visual frame that all subsequent phases fill with functionality. Without it, there is no navigable UI.
Output: A working app shell with tab navigation, resizable split-pane, theme toggle, and placeholder panel content.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-react-app-shell/02-CONTEXT.md
@.planning/phases/02-react-app-shell/02-RESEARCH.md
@src/stores/configStore.js
@src/styles/globals.css

<interfaces>
<!-- Existing configStore API the executor needs -->
From src/stores/configStore.js:
```javascript
// React hook usage:
import useConfigStore from '../../stores/configStore';
const debugMode = useConfigStore((s) => s.app.debugMode);
const setDebugMode = useConfigStore((s) => s.setDebugMode);

// Available state shape:
// { api: { text: {baseUrl, apiKey, model, ...}, image: {...}, sillytavern: {...} },
//   app: { maxRetries, retryDelay, debugMode, persistApiKeys, enableImageGeneration },
//   prompts: { contentPolicyPrefix, overrides },
//   debugMode, set(), get(), saveConfig(), setDebugMode(), ... }
```

From src/styles/globals.css (theme system):
```css
:root { /* light theme variables */ }
[data-theme="dark"] { /* dark theme variable overrides */ }
[data-theme="dark"] .header { /* component-specific dark overrides */ }
```

From src/main.jsx:
```javascript
import './styles/globals.css';  // Global styles already imported here
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependency, create useTheme hook, add theme flash prevention, create Header and TabBar components</name>
  <files>
    package.json,
    index.html,
    src/hooks/useTheme.js,
    src/components/layout/Header.jsx,
    src/components/layout/Header.module.css,
    src/components/layout/TabBar.jsx,
    src/components/layout/TabBar.module.css
  </files>
  <read_first>
    src/styles/globals.css,
    src/stores/configStore.js,
    index.html,
    package.json,
    .planning/phases/02-react-app-shell/02-RESEARCH.md
  </read_first>
  <action>
1. Run `npm install react-resizable-panels` to add the split-pane library (per D-05).

2. Create `src/hooks/useTheme.js`:
   - useState initialized from `localStorage.getItem('theme') || 'light'`
   - useEffect sets `document.documentElement.dataset.theme = theme` and persists to localStorage
   - toggleTheme callback toggles between 'dark' and 'light'
   - Export: `{ theme, toggleTheme, isDark }` (per D-10, REACT-10)

3. Add inline theme flash prevention script to `index.html` BEFORE the module script tag:
   ```html
   <script>
     const t = localStorage.getItem('theme');
     if (t) document.documentElement.dataset.theme = t;
   </script>
   ```
   This prevents the white flash when loading in dark mode (per Research pitfall 3).

4. Create `src/components/layout/Header.jsx` (per D-01):
   - Props: `onSettingsClick`, `onThemeToggle`, `isDark`, `onLibraryToggle`
   - Renders: app title "Character Generator", settings gear icon button (calls onSettingsClick), debug toggle button (reads/writes configStore debugMode via selector `useConfigStore((s) => s.app.debugMode)` and `useConfigStore((s) => s.setDebugMode)`), theme toggle button (calls onThemeToggle, shows sun/moon based on isDark), library toggle button (calls onLibraryToggle) (per D-04)
   - Use simple unicode/text for icons: gear = settings icon (Unicode &#9881; or text "Settings"), theme = sun/moon emoji or text, debug = bug icon or text "Debug", library = book icon or text "Library"
   - Import styles from `./Header.module.css`

5. Create `src/components/layout/Header.module.css`:
   - Extract `.header` styles from globals.css (lines 71-82): padding, border-radius, border, background gradient, box-shadow, backdrop-filter
   - Add flex layout: `display: flex; align-items: center; justify-content: space-between`
   - Add `.title` styles from globals.css (lines 84-89)
   - Add `.controls` for the right-side button group: `display: flex; gap: 0.5rem; align-items: center`
   - Add `.iconBtn` for header buttons: minimal styling, cursor pointer, background none, border none, color var(--text-secondary), font-size 1.2rem, hover color var(--text-primary)
   - Add `.iconBtn.active` for active state (debug toggle when on): color var(--accent)
   - Add dark theme override: `:global([data-theme="dark"]) .header { border-color: rgba(255,255,255,0.07); background: linear-gradient(120deg, rgba(22,32,48,0.92), rgba(16,24,38,0.82)); }`

6. Create `src/components/layout/TabBar.jsx` (per D-01, D-06 — only Create and Edit tabs, no Settings tab):
   - Props: `activeTab`, `onTabChange`
   - TABS constant: `[{ id: 'create', label: 'Create' }, { id: 'edit', label: 'Edit' }]`
   - Each button: `role="tab"`, `aria-selected={activeTab === tab.id}`, onClick calls onTabChange(tab.id)
   - Active tab gets the active class
   - Import styles from `./TabBar.module.css`

7. Create `src/components/layout/TabBar.module.css`:
   - Extract `.tab-bar` styles from globals.css (lines 103-108): flex, border-bottom
   - Extract `.tab-btn` styles (lines 110-142): padding, font, color, border-bottom transition, hover, active states
   - Use `.tabBar`, `.tabBtn`, `.active` as camelCase class names in the module
   - Add dark override: `:global([data-theme="dark"]) .tabBar { border-color: rgba(255,255,255,0.09); }`
  </action>
  <verify>
    <automated>cd /Users/dgibbons/git/character-card-generator && node -e "require('react-resizable-panels')" && test -f src/hooks/useTheme.js && test -f src/components/layout/Header.jsx && test -f src/components/layout/Header.module.css && test -f src/components/layout/TabBar.jsx && test -f src/components/layout/TabBar.module.css && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "react-resizable-panels" package.json` succeeds
    - `grep -q "useTheme" src/hooks/useTheme.js` succeeds
    - `grep -q "dataset.theme" index.html` succeeds (flash prevention script)
    - `grep -q "onSettingsClick" src/components/layout/Header.jsx` succeeds
    - `grep -q "onThemeToggle" src/components/layout/Header.jsx` succeeds
    - `grep -q "onLibraryToggle" src/components/layout/Header.jsx` succeeds
    - `grep -q 'role="tab"' src/components/layout/TabBar.jsx` succeeds
    - `grep -q "aria-selected" src/components/layout/TabBar.jsx` succeeds
    - `grep -q "data-theme" src/components/layout/Header.module.css` succeeds (dark override)
  </acceptance_criteria>
  <done>react-resizable-panels installed. useTheme hook created with localStorage persistence. Theme flash prevention script in index.html. Header component with title, gear, debug, theme, library buttons. TabBar with Create/Edit tabs using ARIA roles. Both have CSS Module files with styles extracted from globals.css including dark theme overrides.</done>
</task>

<task type="auto">
  <name>Task 2: Create ActionBar, ProgressBar, SplitPane components and wire complete App.jsx layout</name>
  <files>
    src/components/layout/ActionBar.jsx,
    src/components/layout/ActionBar.module.css,
    src/components/common/ProgressBar.jsx,
    src/components/common/ProgressBar.module.css,
    src/components/layout/SplitPane.jsx,
    src/components/layout/SplitPane.module.css,
    src/App.jsx
  </files>
  <read_first>
    src/components/layout/Header.jsx,
    src/components/layout/TabBar.jsx,
    src/hooks/useTheme.js,
    src/styles/globals.css,
    src/main.jsx,
    .planning/phases/02-react-app-shell/02-RESEARCH.md
  </read_first>
  <action>
1. Create `src/components/common/ProgressBar.jsx`:
   - Props: `active` (boolean, default false) — when true shows animated indeterminate bar, when false the bar is hidden
   - Renders a container div with an inner animated bar div
   - Import styles from `./ProgressBar.module.css`

2. Create `src/components/common/ProgressBar.module.css`:
   - Extract `.generation-progress` and `.generation-progress-bar` from globals.css (lines 1561-1582)
   - Use `.track` for the outer container (height: 4px, border-radius: 2px, background: var(--border), overflow: hidden)
   - Use `.bar` for the inner animated element (height: 100%, width: 40%, border-radius: 2px, background: var(--accent), animation: progress-slide 1.4s ease-in-out infinite)
   - `.hidden` class: `visibility: hidden` (keeps layout space but hides bar when not active)
   - Include the `@keyframes progress-slide` animation

3. Create `src/components/layout/ActionBar.jsx` (per D-02):
   - Props: `isGenerating` (boolean, default false)
   - Renders a sticky bar with:
     - Button group: Generate button (disabled, placeholder for Phase 3), Evaluate button (disabled), Revise button (disabled)
     - ProgressBar component with `active={isGenerating}`
   - All buttons are `disabled` with no click handlers (Phase 3 will enable them)
   - Use class names from globals.css for buttons: `btn-primary` for Generate, `btn-outline` for Evaluate/Revise (these styles are already in globals.css and don't need extraction)
   - Import styles from `./ActionBar.module.css`

4. Create `src/components/layout/ActionBar.module.css`:
   - `.actionBar`: display flex, align-items center, gap 0.6rem, padding 0.6rem 0, position sticky, top 0, z-index 10, background var(--bg-page)
   - `.btnGroup`: display flex, gap 0.5rem
   - Dark override: `:global([data-theme="dark"]) .actionBar { background: var(--bg-page); }`

5. Create `src/components/layout/SplitPane.jsx` (per D-03, D-05):
   - Props: `leftContent` (ReactNode), `rightContent` (ReactNode)
   - Uses `PanelGroup`, `Panel`, `PanelResizeHandle` from `react-resizable-panels`
   - PanelGroup: `direction="horizontal"`, `autoSaveId="main-split"` (persists resize ratio to localStorage per D-05)
   - Left Panel: `defaultSize={50}`, `minSize={20}`
   - Right Panel: `defaultSize={50}`, `minSize={20}`
   - PanelResizeHandle: styled as a vertical divider
   - Each panel content wrapped in a div with overflow-y auto and padding
   - Import styles from `./SplitPane.module.css`

6. Create `src/components/layout/SplitPane.module.css`:
   - `.wrapper`: flex 1, min-height 0, display flex, flex-direction column (ensures PanelGroup gets sized container — per Research pitfall 4)
   - `.panel`: height 100%, overflow-y auto, padding 0.5rem
   - `.handle`: width 6px, background var(--border), border-radius 3px, transition background 150ms, cursor col-resize
   - `.handle:hover`: background var(--accent)
   - Dark override for handle: `:global([data-theme="dark"]) .handle { background: rgba(255,255,255,0.12); }`

7. Replace `src/App.jsx` with the full layout wiring:
   - Import: Header, TabBar, ActionBar, SplitPane from components/layout/
   - Import: useTheme from hooks/useTheme
   - Import: useState, useCallback from react
   - State: `activeTab` ('create' default), `settingsOpen` (false), `libraryOpen` (false)
   - useTheme() for theme/toggleTheme/isDark
   - leftPanel: conditional on activeTab — 'create' shows `<div>Create form placeholder</div>`, 'edit' shows `<div>Edit form placeholder</div>` (per D-03, these are placeholders for Phase 3)
   - rightPanel: `<div>Character preview placeholder</div>`
   - Layout structure (per D-01, D-02, D-03):
     ```
     <div className="container">
       <Header onSettingsClick={() => setSettingsOpen(true)} onThemeToggle={toggleTheme} isDark={isDark} onLibraryToggle={() => setLibraryOpen(prev => !prev)} />
       <TabBar activeTab={activeTab} onTabChange={setActiveTab} />
       <ActionBar />
       <SplitPane leftContent={leftPanel} rightContent={rightPanel} />
     </div>
     ```
   - Note: SettingsModal will be added in Plan 02. For now, settingsOpen state exists but no modal rendered yet.
   - Note: Library drawer will be added in Phase 4. For now, libraryOpen state exists but no drawer rendered.
  </action>
  <verify>
    <automated>cd /Users/dgibbons/git/character-card-generator && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "PanelGroup" src/components/layout/SplitPane.jsx` succeeds
    - `grep -q "autoSaveId" src/components/layout/SplitPane.jsx` succeeds
    - `grep -q "ProgressBar" src/components/layout/ActionBar.jsx` succeeds
    - `grep -q "disabled" src/components/layout/ActionBar.jsx` succeeds (buttons disabled)
    - `grep -q "Header" src/App.jsx` succeeds
    - `grep -q "TabBar" src/App.jsx` succeeds
    - `grep -q "ActionBar" src/App.jsx` succeeds
    - `grep -q "SplitPane" src/App.jsx` succeeds
    - `grep -q "useTheme" src/App.jsx` succeeds
    - `npx vite build` succeeds with no errors
  </acceptance_criteria>
  <done>ActionBar with disabled Generate/Evaluate/Revise buttons and ProgressBar. SplitPane using react-resizable-panels with autoSaveId persistence. App.jsx wires Header + TabBar + ActionBar + SplitPane into the full layout. Vite build passes with no errors.</done>
</task>

</tasks>

<verification>
1. `npx vite build` completes without errors
2. `npm run dev` starts and the app loads in browser showing header, tabs, action bar, and split pane
3. Theme toggle switches between light and dark mode
4. Tab switching changes the left panel placeholder text
5. Split pane resize handle works and ratio persists on reload
6. No theme flash when loading in dark mode
</verification>

<success_criteria>
- All layout components exist with CSS Modules
- App.jsx renders the complete shell layout
- Theme switching works with localStorage persistence
- Split-pane resizes and persists via react-resizable-panels autoSaveId
- Tab navigation switches left panel content
- Action bar shows disabled buttons with progress bar
- Vite build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-react-app-shell/02-01-SUMMARY.md`
</output>
