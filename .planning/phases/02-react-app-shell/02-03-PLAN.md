---
phase: 02-react-app-shell
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/styles/globals.css
autonomous: false

requirements:
  - CSS-03

must_haves:
  truths:
    - "Visual appearance matches existing design with no regressions in layout, colors, spacing"
    - "Extracted CSS rules removed from globals.css to prevent duplicate styling"
    - "Dark theme works correctly on all shell components"
  artifacts:
    - path: "src/styles/globals.css"
      provides: "Cleaned globals.css without duplicated shell component styles"
  key_links:
    - from: "src/styles/globals.css"
      to: "src/components/layout/Header.module.css"
      via: "No duplicate .header rules — extracted to module"
      pattern: "header"
---

<objective>
Clean up globals.css by removing CSS rules that were extracted into CSS Modules in Plans 01 and 02. Verify visual appearance matches the original design with no regressions.

Purpose: Prevent CSS duplication pitfall (Research pitfall 1) where both global and module styles apply to the same elements. Ensure CSS-03 (visual parity) is satisfied.
Output: Cleaned globals.css and verified visual appearance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-react-app-shell/02-CONTEXT.md
@.planning/phases/02-react-app-shell/02-RESEARCH.md
@.planning/phases/02-react-app-shell/02-01-SUMMARY.md
@.planning/phases/02-react-app-shell/02-02-SUMMARY.md
@src/styles/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove extracted CSS rules from globals.css to prevent duplication</name>
  <files>src/styles/globals.css</files>
  <read_first>
    src/styles/globals.css,
    src/components/layout/Header.module.css,
    src/components/layout/TabBar.module.css,
    src/components/layout/ActionBar.module.css,
    src/components/layout/SplitPane.module.css,
    src/components/common/ProgressBar.module.css,
    src/components/common/Modal.module.css,
    src/components/settings/SettingsModal.module.css,
    src/components/settings/ApiSettings.module.css,
    src/components/settings/AppSettings.module.css
  </read_first>
  <action>
Read every CSS Module file created in Plans 01 and 02. For each rule that was extracted into a CSS Module, remove (or comment out) the corresponding rule from globals.css.

**Rules to remove from globals.css (verify against actual module files before removing):**

From layout components (Plan 01):
- `.header` rule (around line 71-82) — extracted to Header.module.css
- `.tab-bar` rule (around line 103-108) — extracted to TabBar.module.css
- `.tab-btn`, `.tab-btn:hover`, `.tab-btn.active`, `.tab-btn:disabled`, `.tab-btn:disabled:hover` (lines 110-142) — extracted to TabBar.module.css
- `.split-pane`, `.split-left`, `.split-right`, `.split-divider` rules (lines 235-269) — replaced by react-resizable-panels + SplitPane.module.css
- `.generation-progress`, `.generation-progress-bar`, `@keyframes progress-slide` (lines 1561-1582) — extracted to ProgressBar.module.css

From modal components (Plan 02):
- `.modal-overlay`, `.modal-overlay.show`, `@keyframes fadeIn` (lines 929-952) — extracted to Modal.module.css
- `.api-settings-modal` (lines 954-962) — extracted to Modal.module.css
- `.modal-header` (lines 964-976) — extracted to Modal.module.css
- `.modal-title` (lines 978-981) — extracted to Modal.module.css
- `.modal-close`, `.modal-close:hover` (lines 983-998) — extracted to Modal.module.css
- `.modal-body` (lines 1000-1002) — extracted to Modal.module.css

From dark theme section:
- `[data-theme="dark"] .header` (line 1268-1275) — now in Header.module.css
- `[data-theme="dark"] .api-settings-modal` (lines 1309-1312) — now in Modal.module.css
- `[data-theme="dark"] .modal-header` (lines 1314-1317) — now in Modal.module.css
- `[data-theme="dark"] .modal-close`, hover (lines 1319-1325) — now in Modal.module.css
- `[data-theme="dark"] .modal-overlay` (lines 1327-1329) — now in Modal.module.css
- `[data-theme="dark"] .tab-bar` (lines 1351-1353) — now in TabBar.module.css
- `[data-theme="dark"] .split-divider` rules (lines 1359-1366) — now in SplitPane.module.css

From mobile media queries:
- `.header` references in `@media (max-width: 700px)` (line 1423) — extracted
- `.split-pane` references in `@media (max-width: 700px)` (line 1433) — replaced by react-resizable-panels
- `.split-left`, `.split-right`, `.split-divider` mobile rules — replaced
- `.tab-btn` mobile rules (line 1475) — extracted
- `.modal-body`, `.api-settings-modal` mobile rules (lines 1550-1557) — extracted

**IMPORTANT:** Do NOT remove these — they stay global:
- `:root` variables and `[data-theme="dark"]` root variable overrides (theme system)
- `*`, `html`, `body`, `.container` rules (global layout)
- `.btn-primary`, `.btn-outline`, `.btn-small` (button styles used across components)
- `.form-group`, `.form-row`, `.form-label` (form styles used in settings and future phases)
- `.switch`, `.slider` (toggle switch styles used in settings)
- `.tab-badge`, `.tab-panels`, `.tab-panel` (may still be needed or leave for Phase 3)
- `.title`, `.subtitle` — ONLY remove if they were extracted to Header.module.css. If Header uses different class names (e.g., `.title` in module becomes scoped), keep the global ones for potential use elsewhere.
- Any styles for components NOT yet built in React (library drawer, editor, stream box, etc.)

**Approach:** Add a comment block at the location of each removed section:
```css
/* Extracted to Header.module.css */
/* Extracted to TabBar.module.css */
```
This makes it clear during Phase 3/4 where styles went.

After removing, verify the build still passes and no styles are obviously broken.
  </action>
  <verify>
    <automated>cd /Users/dgibbons/git/character-card-generator && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Extracted to Header.module.css" src/styles/globals.css` succeeds
    - `grep -q "Extracted to TabBar.module.css" src/styles/globals.css` succeeds
    - `grep -q "Extracted to Modal.module.css" src/styles/globals.css` succeeds
    - `grep -c "\.header {" src/styles/globals.css` returns 0 (rule removed)
    - `grep -c "\.modal-overlay {" src/styles/globals.css` returns 0 (rule removed)
    - `grep -c "\.tab-bar {" src/styles/globals.css` returns 0 (rule removed)
    - `grep -q ":root" src/styles/globals.css` succeeds (global vars preserved)
    - `grep -q "\.btn-primary" src/styles/globals.css` succeeds (button styles preserved)
    - `grep -q "\.switch" src/styles/globals.css` succeeds (toggle styles preserved)
    - `npx vite build` succeeds
  </acceptance_criteria>
  <done>globals.css cleaned of all rules extracted into CSS Modules. Comment markers indicate where each section was extracted to. Global styles (variables, buttons, forms, toggles) preserved. No duplicate rules between globals.css and CSS Modules.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete React app shell</name>
  <files>none</files>
  <action>
Present the complete React application shell for human visual verification. The executor should start the dev server with `npm run dev` if not already running, then present the verification checklist to the user.

What was built across Plans 01-03:
- Header (title, settings gear, debug toggle, theme toggle, library toggle)
- Tab bar (Create/Edit tabs with conditional left panel content)
- Action bar (disabled Generate/Evaluate/Revise buttons, progress bar)
- Resizable split-pane layout (persists ratio to localStorage)
- Settings modal (API config, app toggles, save/cancel with configStore binding)
- Theme switching (dark/light with no flash on load)
- CSS Modules for all shell components
- globals.css cleaned of duplicated rules
  </action>
  <verify>
Human verifies the following at http://localhost:5173:
1. Layout check: Header, Tab bar, Action bar, Split pane stacked vertically
2. Theme toggle: dark/light switching works, persists on reload, no flash
3. Tab switching: Create/Edit tabs switch left panel placeholder text
4. Split pane: resize handle works, ratio persists on reload
5. Settings modal: gear icon opens it, all API fields editable, toggles work
6. Settings save/cancel: Save persists, Cancel discards
7. Action bar: Generate/Evaluate/Revise buttons visible but disabled
8. Dark theme: modal and all components have dark styling
9. Visual parity: colors, fonts, spacing match existing design
  </verify>
  <done>User approves visual appearance and functionality of the React app shell. All Phase 2 success criteria met: tab navigation works, split-pane resizes and persists, settings save/cancel works, theme switching works with no flash, CSS Modules applied correctly.</done>
</task>

</tasks>

<verification>
1. `npx vite build` succeeds with no errors
2. All shell components render correctly in browser
3. Theme toggle works and persists
4. Tab navigation switches content
5. Split pane resizes and persists
6. Settings modal save/cancel works correctly
7. No duplicate CSS rules between globals.css and CSS Modules
8. Dark theme works on all components
</verification>

<success_criteria>
- globals.css cleaned of extracted rules with marker comments
- Visual appearance matches existing design (CSS-03)
- User confirms the app shell looks and functions correctly
- No CSS duplication causing style conflicts
</success_criteria>

<output>
After completion, create `.planning/phases/02-react-app-shell/02-03-SUMMARY.md`
</output>
