---
phase: 02-react-app-shell
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/common/Modal.jsx
  - src/components/common/Modal.module.css
  - src/components/settings/SettingsModal.jsx
  - src/components/settings/SettingsModal.module.css
  - src/components/settings/ApiSettings.jsx
  - src/components/settings/ApiSettings.module.css
  - src/components/settings/AppSettings.jsx
  - src/components/settings/AppSettings.module.css
  - src/App.jsx
autonomous: true
requirements:
  - REACT-04
  - STATE-01
  - STATE-05

must_haves:
  truths:
    - "User can open settings modal by clicking gear icon in header"
    - "User can edit API settings (text API base URL, key, model; image API base URL, key, model; SillyTavern URL, password)"
    - "User can toggle app settings (debug mode, persist API keys, content policy prefix, enable image generation)"
    - "User can save settings and values persist to configStore/localStorage"
    - "User can cancel settings and unsaved changes are discarded"
    - "API keys persist in sessionStorage by default, localStorage when persistApiKeys is enabled"
  artifacts:
    - path: "src/components/common/Modal.jsx"
      provides: "Reusable modal overlay shell"
      exports: ["default"]
    - path: "src/components/settings/SettingsModal.jsx"
      provides: "Settings form with save/cancel"
      exports: ["default"]
    - path: "src/components/settings/ApiSettings.jsx"
      provides: "API configuration form sections"
      exports: ["default"]
    - path: "src/components/settings/AppSettings.jsx"
      provides: "App toggle settings"
      exports: ["default"]
  key_links:
    - from: "src/App.jsx"
      to: "src/components/settings/SettingsModal.jsx"
      via: "isOpen/onClose props driven by settingsOpen state"
      pattern: "settingsOpen"
    - from: "src/components/settings/SettingsModal.jsx"
      to: "src/stores/configStore.js"
      via: "reads store on open, writes on save via store.set() and saveConfig()"
      pattern: "useConfigStore"
    - from: "src/components/settings/SettingsModal.jsx"
      to: "src/stores/configStore.js"
      via: "calls updateStorageMethod() when persistApiKeys changes"
      pattern: "updateStorageMethod"
---

<objective>
Build the settings modal with save/cancel pattern, API configuration forms, and app toggle settings that read from and write to the existing Zustand configStore.

Purpose: Users need to configure API endpoints, keys, and app preferences before any generation can happen. The settings modal is the gateway to all functionality.
Output: A working settings modal accessible from the header gear icon, with all config fields binding to the Zustand store.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-react-app-shell/02-CONTEXT.md
@.planning/phases/02-react-app-shell/02-RESEARCH.md
@.planning/phases/02-react-app-shell/02-01-SUMMARY.md
@src/stores/configStore.js

<interfaces>
<!-- configStore API needed for settings modal -->
From src/stores/configStore.js:
```javascript
import useConfigStore from '../../stores/configStore';

// Read entire state for draft copy:
const storeState = useConfigStore();
// storeState.api.text.baseUrl, .apiKey, .model, .visionModel, .timeout
// storeState.api.image.baseUrl, .apiKey, .model, .size, .timeout
// storeState.api.sillytavern.url, .password
// storeState.app.debugMode, .persistApiKeys, .enableImageGeneration, .maxRetries, .retryDelay
// storeState.prompts.contentPolicyPrefix

// Write individual fields:
storeState.set('api.text.baseUrl', value);
storeState.set('api.text.apiKey', value);
storeState.set('app.persistApiKeys', value);
storeState.set('prompts.contentPolicyPrefix', value);
// ... etc for all fields

// Persist after all sets:
storeState.saveConfig();

// When persistApiKeys changes, migrate keys between session/localStorage:
storeState.updateStorageMethod();
```

<!-- Header component from Plan 01 (gear icon triggers settings) -->
From src/components/layout/Header.jsx:
```javascript
// Header accepts onSettingsClick prop — calls it when gear icon clicked
export default function Header({ onSettingsClick, onThemeToggle, isDark, onLibraryToggle })
```

<!-- App.jsx state from Plan 01 -->
From src/App.jsx:
```javascript
const [settingsOpen, setSettingsOpen] = useState(false);
// Header's onSettingsClick={() => setSettingsOpen(true)} already wired
// SettingsModal needs to be added to App.jsx render
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reusable Modal component and SettingsModal with save/cancel pattern</name>
  <files>
    src/components/common/Modal.jsx,
    src/components/common/Modal.module.css,
    src/components/settings/SettingsModal.jsx,
    src/components/settings/SettingsModal.module.css
  </files>
  <read_first>
    src/stores/configStore.js,
    src/styles/globals.css,
    src/App.jsx,
    .planning/phases/02-react-app-shell/02-RESEARCH.md
  </read_first>
  <action>
1. Create `src/components/common/Modal.jsx`:
   - Props: `isOpen` (boolean), `onClose` (function), `title` (string), `children` (ReactNode)
   - When `!isOpen`, return null
   - Renders overlay div (fixed, full screen) with click-to-close on overlay background
   - Inner modal container stops click propagation
   - Sticky header with title and close button (X)
   - Body section renders children
   - Escape key closes: useEffect adds keydown listener for 'Escape' calling onClose
   - Import styles from `./Modal.module.css`

2. Create `src/components/common/Modal.module.css`:
   - Extract from globals.css:
     - `.overlay`: from `.modal-overlay` (lines 929-943) — position fixed, inset 0, background rgba(14,23,37,0.42), display flex, align-items center, justify-content center, padding 1rem, z-index 1100, animation fadeIn 140ms ease-out
     - `.modal`: from `.api-settings-modal` (lines 954-962) — width min(760px,100%), max-height min(88vh,980px), overflow auto, border-radius var(--radius-lg), border 1px solid rgba(255,255,255,0.45), background linear-gradient(180deg,#ffffff,#f8fbff), box-shadow var(--shadow-lg)
     - `.header`: from `.modal-header` (lines 964-976) — sticky top 0, z-index 3, padding 1rem 1.2rem, border-bottom, background rgba(255,255,255,0.92), backdrop-filter blur(6px), flex, space-between
     - `.title`: from `.modal-title` (lines 978-981)
     - `.closeBtn`: from `.modal-close` (lines 983-998) — 34px circle, border, cursor pointer
     - `.body`: from `.modal-body` (lines 1000-1002) — padding 1rem 1.2rem
   - Dark overrides using `:global([data-theme="dark"])`:
     - `.overlay` background: rgba(0,0,0,0.6)
     - `.modal` background: linear-gradient(180deg,#1a2637,#141e2e), border-color rgba(255,255,255,0.1)
     - `.header` background: rgba(20,30,46,0.96)
     - `.closeBtn` background: var(--surface-strong), hover #223044
   - Include `@keyframes fadeIn` in this file

3. Create `src/components/settings/SettingsModal.jsx` (per D-06, D-07):
   - Props: `isOpen` (boolean), `onClose` (function)
   - Uses Modal component with title "Settings"
   - **Draft state pattern (per D-07 — explicit save, not auto-save):**
     - `const [draft, setDraft] = useState(null)`
     - useEffect keyed on `isOpen`: when modal opens, deep-copy relevant store state into draft:
       ```javascript
       const state = useConfigStore.getState();
       setDraft({
         api: JSON.parse(JSON.stringify(state.api)),
         app: { ...state.app },
         prompts: { ...state.prompts },
       });
       ```
     - This prevents stale state (Research pitfall 2)
   - `updateDraft(path, value)` helper: clones draft, uses setNestedValue-style logic to update nested path, calls setDraft
   - **Save handler:**
     - Gets store's set function: `const storeSet = useConfigStore.getState().set`
     - Iterates over all draft fields and calls `storeSet(path, value)` for each changed field
     - Specifically set these paths from draft:
       - `api.text.baseUrl`, `api.text.apiKey`, `api.text.model`, `api.text.visionModel`, `api.text.timeout`
       - `api.image.baseUrl`, `api.image.apiKey`, `api.image.model`, `api.image.size`, `api.image.timeout`
       - `api.sillytavern.url`, `api.sillytavern.password`
       - `app.debugMode`, `app.persistApiKeys`, `app.enableImageGeneration`, `app.maxRetries`, `app.retryDelay`
       - `prompts.contentPolicyPrefix`
     - Calls `useConfigStore.getState().saveConfig()` after all sets
     - If `draft.app.persistApiKeys !== originalPersistApiKeys`, calls `useConfigStore.getState().updateStorageMethod()` to migrate keys (per STATE-05)
     - Calls `onClose()`
   - **Cancel handler:** calls `onClose()` — draft is discarded (per D-07)
   - Renders:
     - ApiSettings component with draft and updateDraft
     - AppSettings component with draft and updateDraft
     - Footer with Save button (class `btn-primary`) and Cancel button (class `btn-outline`)
   - Import styles from `./SettingsModal.module.css`

4. Create `src/components/settings/SettingsModal.module.css`:
   - `.footer`: display flex, justify-content flex-end, gap 0.6rem, padding 1rem 1.2rem, border-top 1px solid var(--border)
   - `.section`: margin-bottom 1.5rem
   - `.sectionTitle`: font-size 0.9rem, font-weight 600, color var(--text-secondary), text-transform uppercase, letter-spacing 0.05em, margin-bottom 0.8rem
  </action>
  <verify>
    <automated>cd /Users/dgibbons/git/character-card-generator && test -f src/components/common/Modal.jsx && test -f src/components/settings/SettingsModal.jsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "isOpen" src/components/common/Modal.jsx` succeeds
    - `grep -q "Escape" src/components/common/Modal.jsx` succeeds (keyboard close)
    - `grep -q "draft" src/components/settings/SettingsModal.jsx` succeeds (draft state pattern)
    - `grep -q "saveConfig" src/components/settings/SettingsModal.jsx` succeeds (save persists)
    - `grep -q "updateStorageMethod" src/components/settings/SettingsModal.jsx` succeeds (key migration)
    - `grep -q "onClose" src/components/settings/SettingsModal.jsx` succeeds (cancel discards)
    - `grep -q "fadeIn" src/components/common/Modal.module.css` succeeds (animation)
    - `grep -q "data-theme" src/components/common/Modal.module.css` succeeds (dark overrides)
  </acceptance_criteria>
  <done>Reusable Modal with overlay, escape-to-close, dark theme support. SettingsModal with draft state pattern: copies store on open, saves all fields on Save, discards on Cancel. Calls updateStorageMethod when persistApiKeys changes.</done>
</task>

<task type="auto">
  <name>Task 2: Create ApiSettings and AppSettings form components, wire SettingsModal into App.jsx</name>
  <files>
    src/components/settings/ApiSettings.jsx,
    src/components/settings/ApiSettings.module.css,
    src/components/settings/AppSettings.jsx,
    src/components/settings/AppSettings.module.css,
    src/App.jsx
  </files>
  <read_first>
    src/components/settings/SettingsModal.jsx,
    src/components/common/Modal.jsx,
    src/stores/configStore.js,
    src/App.jsx,
    src/styles/globals.css
  </read_first>
  <action>
1. Create `src/components/settings/ApiSettings.jsx`:
   - Props: `draft` (the draft state object), `updateDraft` (function(path, value))
   - Renders three form sections using global CSS classes (`form-group`, `form-row` from globals.css — do NOT extract these to modules, they stay global):

   **Text API section:**
   - Base URL: `<input type="text" value={draft.api.text.baseUrl} onChange={e => updateDraft('api.text.baseUrl', e.target.value)} />`
   - API Key: `<input type="password" value={draft.api.text.apiKey} onChange={...} />`
   - Model: `<input type="text" value={draft.api.text.model} onChange={...} />`
   - Vision Model: `<input type="text" value={draft.api.text.visionModel} onChange={...} />`
   - Timeout: `<input type="number" value={draft.api.text.timeout} onChange={e => updateDraft('api.text.timeout', parseInt(e.target.value) || 180000)} />`

   **Image API section:**
   - Base URL, API Key (password), Model, Size, Timeout — same pattern as text API but for `draft.api.image.*`

   **SillyTavern section:**
   - URL: text input for `draft.api.sillytavern.url`
   - Password: password input for `draft.api.sillytavern.password`

   - Each section has a heading (e.g., "Text API", "Image API", "SillyTavern")
   - Import styles from `./ApiSettings.module.css`

2. Create `src/components/settings/ApiSettings.module.css`:
   - `.section`: margin-bottom 1.2rem
   - `.sectionHeading`: font-size 0.95rem, font-weight 600, margin-bottom 0.6rem, color var(--text-primary)
   - `.field`: margin-bottom 0.6rem
   - `.label`: display block, font-size 0.82rem, font-weight 500, color var(--text-secondary), margin-bottom 0.25rem
   - `.input`: width 100%, padding 0.5rem 0.7rem, font inherit, font-size 0.9rem, border 1px solid var(--border), border-radius var(--radius-sm), background var(--surface-strong), color var(--text-primary), transition border-color var(--transition)
   - `.input:focus`: outline none, border-color var(--accent), box-shadow 0 0 0 3px rgba(14,122,138,0.12)
   - Dark override for input: `:global([data-theme="dark"]) .input { background: var(--surface-muted); border-color: var(--border); }`

3. Create `src/components/settings/AppSettings.jsx`:
   - Props: `draft`, `updateDraft`
   - Renders toggle switches for boolean settings using the existing global `.switch` + `.slider` CSS classes (lines 1009-1054 of globals.css — keep these global):

   **Toggle rows (each row: label + switch):**
   - Debug Mode: `checked={draft.app.debugMode}`, onChange toggles `app.debugMode`
   - Persist API Keys: `checked={draft.app.persistApiKeys}`, onChange toggles `app.persistApiKeys`. Add helper text: "Store API keys in localStorage instead of sessionStorage (persists across browser sessions)"
   - Enable Image Generation: `checked={draft.app.enableImageGeneration}`, onChange toggles `app.enableImageGeneration`
   - Content Policy Prefix: `checked={draft.prompts.contentPolicyPrefix}`, onChange toggles `prompts.contentPolicyPrefix`. Add helper text: "Prepend content policy bypass to prompts"

   - Import styles from `./AppSettings.module.css`

4. Create `src/components/settings/AppSettings.module.css`:
   - `.settingRow`: display flex, align-items center, justify-content space-between, padding 0.6rem 0, border-bottom 1px solid var(--border)
   - `.settingRow:last-child`: border-bottom none
   - `.settingInfo`: flex 1
   - `.settingLabel`: font-size 0.9rem, font-weight 500, color var(--text-primary)
   - `.settingHelp`: font-size 0.78rem, color var(--text-soft), margin-top 0.15rem

5. Update `src/App.jsx`:
   - Add import: `import SettingsModal from './components/settings/SettingsModal'`
   - Add `<SettingsModal isOpen={settingsOpen} onClose={() => setSettingsOpen(false)} />` inside the container div, after SplitPane
   - The settingsOpen state and Header's onSettingsClick should already be wired from Plan 01
  </action>
  <verify>
    <automated>cd /Users/dgibbons/git/character-card-generator && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "updateDraft" src/components/settings/ApiSettings.jsx` succeeds
    - `grep -q "api.text.baseUrl" src/components/settings/ApiSettings.jsx` succeeds
    - `grep -q "api.image.baseUrl" src/components/settings/ApiSettings.jsx` succeeds
    - `grep -q "api.sillytavern.url" src/components/settings/ApiSettings.jsx` succeeds
    - `grep -q "type=\"password\"" src/components/settings/ApiSettings.jsx` succeeds (API keys masked)
    - `grep -q "persistApiKeys" src/components/settings/AppSettings.jsx` succeeds
    - `grep -q "contentPolicyPrefix" src/components/settings/AppSettings.jsx` succeeds
    - `grep -q "enableImageGeneration" src/components/settings/AppSettings.jsx` succeeds
    - `grep -q "SettingsModal" src/App.jsx` succeeds
    - `grep -q "settingsOpen" src/App.jsx` succeeds
    - `npx vite build` succeeds with no errors
  </acceptance_criteria>
  <done>ApiSettings renders all API configuration fields (text, image, SillyTavern) with proper input types. AppSettings renders toggle switches for debug mode, persist API keys, image generation, and content policy. SettingsModal wired into App.jsx via gear icon. Vite build passes.</done>
</task>

</tasks>

<verification>
1. `npx vite build` completes without errors
2. Click gear icon in header opens settings modal
3. All API fields are editable (text, image, SillyTavern sections)
4. Toggle switches work for debug mode, persist API keys, image gen, content policy
5. Save button persists changes to configStore (verify in localStorage)
6. Cancel button discards changes (verify values unchanged)
7. Escape key and overlay click close the modal
8. Modal has proper dark theme styling
</verification>

<success_criteria>
- Settings modal opens from header gear icon and closes via Cancel/Escape/overlay click
- All API configuration fields bind to draft state
- Save writes all fields to configStore and calls saveConfig()
- Cancel discards draft without modifying store
- persistApiKeys toggle triggers updateStorageMethod() on save
- All form components use CSS Modules with dark theme support
- Vite build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-react-app-shell/02-02-SUMMARY.md`
</output>
